{
  "name": "next-week-with-todoist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc",
    "postbuild": "cp -R node_modules ./dist/node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.59",
    "@types/dotenv": "^8.2.0",
    "@types/node": "^14.0.1",
    "@types/node-fetch": "^2.5.7"
  },
  "dependencies": {
    "aws-lambda": "^1.0.6",
    "dotenv": "^8.2.0",
    "node-fetch": "^2.6.0"
  }
}
